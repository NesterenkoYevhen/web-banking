apiVersion: apps/v1
kind: Deployment
metadata:
  name: replenishments-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: replenishments-mongo
  template:
    metadata:
      labels:
        app: replenishments-mongo
    spec:
      containers:
        - name: replenishments-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: replenishments-mongo-srv
spec:
  selector:
    app: replenishments-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017